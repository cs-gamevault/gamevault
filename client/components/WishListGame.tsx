import React from 'react';
import GameInfoModal from '../modals/GameInfoModal';
import DeleteWishListEntryButton from './DeleteWishLishEntryButton';


/**
 * this game element needs to be a clickable element that will trigger its local state to true to render the modal on top of it if selected
 * the modal needs to have an x on it that can reset that state to false to close the modal
 */
const WishListGame = () => {

};

export default WishListGame;